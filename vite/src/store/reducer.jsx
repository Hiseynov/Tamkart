import { createSlice } from "@reduxjs/toolkit";
import { itemsApi } from "../../api/data";

const tamKart = createSlice({
  name: "tamkart",
  initialState: {
  
  },

  reducers: {
  
  },
});

export const {} = tamKart.actions;
export default tamKart.reducer;
